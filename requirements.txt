# Client dependencies. The VLM itself is served elsewhere (vLLM).
openai>=1.30.0
# nothing else is required to run --dry-run or the loader; stdlib csv is used.

# Evaluation pipeline (evaluation/evaluate_predictions.py) only:
numpy
pandas
scipy
matplotlib
